import 'package:flutter/material.dart';

class AppColors {
  // 🌐 Primary (Blue = Trust + Stability)
  static const Color primary = Color(0xFF1E88E5); // Bright Blue
  static const Color primaryDark = Color(0xFF1565C0); // Darker Blue
  static const Color primaryLight = Color(0xFF90CAF9); // Light Blue

  // 💰 Secondary (Green = Finance + Growth)
  static const Color secondary = Color(0xFF43A047); // Balanced Green
  static const Color secondaryDark = Color(0xFF2E7D32); // Deep Green
  static const Color secondaryLight = Color(0xFFA5D6A7); // Light Green

  // ⚠️ Alerts & Status
  static const Color success = Color(0xFF2E7D32); // Success/Positive
  static const Color error = Color(0xFFE53935); // Error/Negative
  static const Color warning = Color(0xFFFDD835); // Warning/Yellow
  static const Color info = Color(0xFF0288D1); // Info/Light Blue

  // 🖤 Neutral / Grayscale
  static const Color black = Color(0xFF000000);
  static const Color darkGray = Color(0xFF424242);
  static const Color gray = Color(0xFF9E9E9E);
  static const Color lightGray = Color(0xFFE0E0E0);
  static const Color white = Color(0xFFFFFFFF);

  // 🎨 Background & Surface
  static const Color background = Color(0xFFF5F5F5); // App background
  static const Color surface = Color(0xFFFFFFFF); // Card / Container background
  static const Color divider = Color(0xFFBDBDBD); // Dividers
}
